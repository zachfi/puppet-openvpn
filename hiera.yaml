---
version: 4
datadir: data
hierarchy:
  - name: "kernel"
    path: "kernel/%{facts.kernel}"
    backend: yaml
  - name: "family"
    path: "os/%{facts.os.family}"
    backend: yaml
  - name: "common"
    backend: yaml
    path: "common"

